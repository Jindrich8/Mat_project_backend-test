{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Task list error response details",
    "description": "Represents every possible error details, except for general error details, that can be thrown by the task list endpoint",
    "type": "object",
    "anyOf": [
        {
            "$ref":"./errors/**/*.json"
        }
    ],
    "required": ["code","errorData"]
}