{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "List my tasks response",
    "description": "Represents a list of my tasks matching requested filters.",
    "properties": {
        "tasks": {
            "type": "array",
            "items": {
                "$ref": "@/defs/types/my_task/preview_info.json"
            }
        },
        "config": {
            "$ref": "@/defs/types/list_config.json"
        }
    },
    "required": [
        "tasks",
        "config"
    ]
}