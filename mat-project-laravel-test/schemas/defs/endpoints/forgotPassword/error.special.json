{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Forgot password error response details",
    "description": "Represents every possible error details, except for general error details and the csrf token mismatch error, that can be thrown by the forgot password endpoint",
    "type": "object",
    "anyOf": [
        {
            "$ref":"./errors/form.json"
        }
    ],
    "required": ["code","errorData"]
}